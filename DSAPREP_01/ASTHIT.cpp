#include<bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin >> t;
    while(t--){
        long long int n;
        cin >> n;
        vector<int>coord;
        vector<int>size;
        for(int i = 0; i < n; i++){
            int x;
            cin >> x;
            coord.push_back(x);
            int y;
            cin >> y;
            size.push_back(y);
        
        
        
        }
    }
    return 0;
}